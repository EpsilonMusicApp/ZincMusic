# Zinc Music baseline profile — speeds up startup and first scroll by
# pre-compiling hot code paths (AOT) instead of JIT-ing them at runtime.
# Format: class rules carry no flags; wildcard then semicolon.
# App startup & navigation
Lcom/zincmusic/app/**;
# Compose runtime + UI pipeline (used heavily during startup & scrolling)
Landroidx/compose/runtime/**;
Landroidx/compose/ui/**;
Landroidx/compose/foundation/**;
Landroidx/compose/animation/**;
Landroidx/compose/material3/**;
Landroidx/activity/compose/**;
Landroidx/lifecycle/runtime/compose/**;
# Coil image pipeline (album art decoding during scroll)
Lcoil/compose/**;
Lcoil/request/**;
Lcoil/decode/**;
Lcoil/memory/**;
Lcoil/map/**;
# Kotlin stdlib hot paths
Lkotlin/jvm/internal/**;
Lkotlin/collections/**;
Lkotlin/text/**;

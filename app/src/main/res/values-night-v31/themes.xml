<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme (Dynamic Color for API 31+ Dark Mode) -->
    <style name="Theme.ZincMusic.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo_static_dark</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.ZincMusic</item>
    </style>
</resources>
